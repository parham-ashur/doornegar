name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend-typecheck:
    name: Frontend typecheck
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - run: npx tsc --noEmit

  backend-tests:
    name: Backend syntax + unit tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Syntax check
        run: python -m compileall -q app
      - name: Install deps
        run: |
          pip install --upgrade pip
          # [llm] is needed because openai/anthropic are imported at module
          # load time by clustering/story_analysis/telegram_analysis services.
          # [nlp] stays out — hazm / sentence-transformers / torch are heavy
          # and guarded with try/except in app.nlp.persian.
          pip install -e ".[dev,llm]"
      - name: Import smoke test
        run: python -c "import app.main"
      - name: Run unit tests
        run: python -m pytest tests/ -q
