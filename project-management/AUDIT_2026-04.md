# Doornegar Audit — April 2026

**Scope**: wide-shallow pass, backend + frontend + infra.
**Method**: parallel Explore agents per dimension, findings verified against repo/git state.
**Tiers**: Blocker (fix before public launch) · Risk (fix before announce) · Nice-to-have.

---

## 1. Security

### Verified state
- `.env`, `.env.backup`, `.env.vercel` are **not** in git history (verified via `git ls-files`) — they live only on Parham's laptop. Severity of secret-exposure findings downgraded accordingly.
- `.gitignore` covers `.env` / `.env.local` / `.env.production` but **not** `.env.backup` or `.env.vercel` — latent risk from a future `git add .`.
- Dashboard client-side password confirmed at `frontend/src/app/[locale]/dashboard/page.tsx:110` — ships in the JS bundle.

### Blockers
| # | Finding | Location | Action |
|---|---|---|---|
| S1 | Dashboard admin password hardcoded client-side (`doornegar2026`) | `frontend/src/app/[locale]/dashboard/page.tsx:110,447` | Remove `ADMIN_PASS`; rely only on backend `ADMIN_TOKEN` sent as `Authorization: Bearer` |
| S2 | `.gitignore` does not cover `.env.*` variants; `.env.backup` & `.env.vercel` are on disk untracked but one `git add .` from being committed | `.gitignore`, `backend/.env.backup`, `frontend/.env.vercel` | Tighten ignore to `.env*` with `!.env.example`; delete the backup; delete the Vercel OIDC file |
| S3 | `POST /api/v1/social/channels` is unauthenticated — anyone can add tracked Telegram channels | `backend/app/api/v1/social.py:38-55` | Add `Depends(require_admin)` |
| S4 | CORS allows stale Vercel preview `frontend-tau-six-36.vercel.app` | Railway env `CORS_ORIGINS` | Lock to `doornegar.org`, `www.doornegar.org`, `localhost:3000` |

### Risks
| # | Finding | Location | Action |
|---|---|---|---|
| S5 | Admin token kept in `localStorage` (XSS-extractable) | `frontend/src/app/[locale]/dashboard/page.tsx:126` | Move to `HttpOnly; Secure; SameSite=Strict` cookie from backend |
| S6 | Admin dashboard endpoints not rate-limited; no lockout on bad password | `backend/app/api/v1/admin.py` | Apply `slowapi` limiter (e.g. 5/min) to `/api/v1/admin/*` |
| S7 | LLM keys and DB creds exist in plaintext on laptop; no rotation playbook | `backend/.env`, `backend/.env.backup` | Document a rotation runbook in project-management; rotate on any laptop compromise |
| S8 | No IP-redaction policy documented for logs (matters for Iranian users) | Railway logs, Cloudflare analytics | Confirm Cloudflare analytics is anonymized; add `X-Redact-IP` policy doc |

### Nice-to-have
- Dependencies are modern (Next 14.2, FastAPI >=0.115, SQLAlchemy 2). Monthly `npm audit` and SDK-version check is enough.
- No raw SQL touching user input — ORM usage is safe.
- Docker Compose dev password (`doornegar_dev`) is intentional and bound to localhost — fine.

---

## 2. Infra & Ops

### Blockers
| # | Finding | Location | Action |
|---|---|---|---|
| I1 | Deploy config lives only in Vercel/Railway dashboards — just caused today's Root Directory drift outage | Vercel project settings | Keep `frontend/vercel.json` as source of truth; document Railway settings in `project-management/INFRA_CONFIG.md` |
| I2 | No CI / pre-commit gate — tests, migrations, and typecheck all run on vibes. Past migration accident fits this pattern. | `.github/workflows/` (did not exist) | **Fixed this session**: `.github/workflows/ci.yml` runs frontend `tsc --noEmit` + backend `compileall` + import smoke-test on every PR to main. Pytest step deferred until B2 test suite exists. |

### Risks
| # | Finding | Location | Action |
|---|---|---|---|
| I3 | `alembic.ini:3` hardcodes localhost DSN; prod is saved by `env.py` override but the file misleads | `backend/alembic.ini` | Replace hardcoded URL with env read or comment explaining the override |
| I4 | No documented backup / restore drill for Neon Postgres | — | Record Neon backup retention; add quarterly restore exercise to `RUNBOOK.md` |
| I5 | No health-check wiring verified on Railway; `/health` exists but may not be hit | `backend/app/main.py` | Confirm Railway healthcheck path in dashboard; mirror in doc |
| I6 | Secrets sync is manual across Railway + Vercel + laptop — drift-prone | — | Either a `scripts/sync_env.sh` wrapper or a documented 3-step checklist |

### Nice-to-have
- `ONBOARDING.md` install list doesn't mention `[nlp,llm]` extras needed for embeddings/bias scoring.
- No Sentry / error aggregation — small project, optional until traffic grows.
- No Cloudflare config-as-code (no wrangler export) — rules live in dashboard.
- `COST_LOG.md` is manual; no alert on spend spike.

---

## 3. Backend code quality

### Verified state
- `Topic`/`TopicArticle` models are **not** dead code (agent initially flagged them) — they are used in `backend/app/api/v1/lab.py` for the `/lab` routes.
- `require_admin` is already fail-closed in production (`admin.py:19-29`): empty token → 403. That downgrades the "admin disabled" warning.

### Blockers
| # | Finding | Location | Action |
|---|---|---|---|
| B1 | Celery tasks have no `time_limit` — a hung LLM call or HTTP fetch can wedge a worker indefinitely | `backend/app/workers/*.py` | **Fixed this session**: per-task `time_limit` / `soft_time_limit` added (10-30 min depending on workload); autoretry intentionally not added because these are batch tasks where re-running costs LLM money |
| B2 | Zero backend test coverage (`backend/tests/` has only conftest) | `backend/tests/` | Add minimal integration tests against a local DB: ingest → cluster → score |

### Risks
| # | Finding | Location | Action |
|---|---|---|---|
| B3 | Broad `except Exception` swallows without logging in several services | `app/services/social_posting.py:35`, `story_analysis.py:328`, `api/v1/stories.py:605`, `api/v1/admin.py:82,116` | **Fixed this session**: high-value silent handlers now `logger.warning(...)`. Narrow expected exceptions (date parsing, JWT) left alone to avoid log noise |
| B4 | Admin endpoints return bare dicts — no Pydantic response_model, so errors leak internal structure | `backend/app/api/v1/admin.py` | Add `response_model=…` per endpoint |
| B5 | Clustering pulls ALL unclustered articles into one LLM call — unbounded on spike days | `backend/app/services/clustering.py` | Add `BATCH_SIZE` hard limit + date-window filter |

### Nice-to-have
- `alembic.ini:3` hardcodes a local Postgres DSN (override works via `env.py`, but the file misleads). Replace with env-read.
- `Pydantic schemas` are good — 31/38 routes have response_model.
- ORM usage is safe; no raw SQL injection surface.

---

## 4. Data pipeline integrity

### Verified state
- Temperature for bias scoring was **0.3** (not deterministic) — confirmed at `bias_scoring.py:397`. Fixed to `0` in the same patch as this audit.
- Ingest loop at `ingestion.py:198` had no per-source try/except — one exception would abort the remaining sources in a run. Wrapped in the same patch.
- Celery beat schedule runs ingest every 15 min, NLP every 15 min, clustering every 30 min, scoring every 60 min (`celery_app.py:24-53`). **No Redis lock on any task** — concurrent runs are possible if one slows.
- `Story.telegram_analysis` is a write-once JSONB cache with **no invalidation path anywhere** in the codebase.

### Blockers
| # | Finding | Location | Status |
|---|---|---|---|
| P1 | Bias scoring `temperature=0.3` broke determinism claim made in prompt | `backend/app/services/bias_scoring.py:397` | **Fixed this session** → `0` |
| P2 | Ingest loop: one crashing source aborted the entire run | `backend/app/services/ingestion.py:198` | **Fixed this session** → per-source try/except |
| P3 | Telegram analysis cache is permanent write-once; new posts never refresh it | `backend/app/api/v1/social.py` | **Fixed this session**: `cached_at` stamp written into the JSONB, 48h TTL on read, `force_refresh=1` admin bypass, new `POST /social/stories/{id}/telegram-analysis/invalidate` admin route |

### Risks
| # | Finding | Location | Action |
|---|---|---|---|
| P4 | Celery beat schedule has no lock — if ingest exceeds 15 min, second run starts mid-commit | `backend/app/workers/celery_app.py:24-53` | **Fixed this session**: new `workers/task_lock.py` helper; `@single_flight` decorator on every beat task with timeout slightly above the task's hard time_limit |
| P5 | Articles stuck with `llm_failed_at` > N days have no dashboard surface — pipeline drift is invisible | `backend/app/models/article.py:51`, admin dashboard | Add "stuck failures >24h" widget |
| P6 | Single unmatched articles (`story_id IS NULL`) are never reassessed after the first clustering attempt | `backend/app/services/clustering.py:720-732` | **Partially fixed this session**: clustering stats now include `aged_orphans` count + WARN log. Full fix (synthetic "miscellaneous" story) deferred — surface is now visible |
| P7 | Dispute score is still the `|state_pct − diaspora_pct|` proxy (already tracked in memory) — LLM-computed version pending | `backend/app/services/clustering.py` | Matches existing project_dispute_score memory — prioritize in next sprint |
| P8 | Deduplication is URL-only; republishes under new URL show as separate articles | `backend/app/models/article.py:26` | Secondary dedup on title+source hash after URL conflict miss |

### Nice-to-have
- `clustering_similarity_threshold=0.45` config and a hardcoded `0.30` pre-filter in `clustering.py:330` — two thresholds in two places. Consolidate.
- Stories have no archive / soft-delete — 30-day-stale stories stay ranked.
- Blindspot threshold (`state_pct < 10`) is a magic number — parameterize + doc.

## 5. Frontend code quality

### Verified state
- `useSearchParams` is used in 3 components; 2 of them (`FeedbackWrapper`, `FeedbackModeBar`) are **dead code** — not imported anywhere. Third is `StoryFeedbackOverlay`, mounted in story detail without a `<Suspense>` boundary.
- Agent flagged `HeaderAnimation.saved.tsx` as orphaned — confirmed, plus the two Feedback* files above.
- Header nav commented out intentionally at `Header.tsx:91-134` ("re-enable by uncommenting") — see UX §6 for user-visible impact.

### Risks
| # | Finding | Location | Action |
|---|---|---|---|
| F1 | `StoryFeedbackOverlay` uses `useSearchParams()` without `<Suspense>` wrapper — forces dynamic rendering of the whole story page + dev-time warnings | `app/[locale]/stories/[id]/page.tsx:94` + `components/improvement/StoryFeedbackOverlay.tsx` | Wrap the overlay usage in `<Suspense fallback={null}>` |
| F2 | 46 `as any` / `: any` escapes concentrated in dashboard pages — renames in API silently break types | dashboard/*, rate/page.tsx, improvement/* | Replace with concrete types from `lib/types.ts` or `unknown` + guards, incrementally |
| F3 | AbortController used in only ~6 of ~60 client fetches — mid-navigation unmount leaks requests | most client fetches | Standardize via a `fetchAPI()` wrapper (one partially exists in `lib/api.ts`, not widely used) |
| F4 | Fetch error-checking inconsistent — some check `res.ok`, some `.status`, some nothing | dashboard/*, improvement/* | Consolidate into the same `fetchAPI()` wrapper |

### Nice-to-have
- **Dead components** (safe to delete): `components/layout/HeaderAnimation.saved.tsx`, `components/feedback/FeedbackWrapper.tsx`, `components/feedback/FeedbackModeBar.tsx`.
- 5 `<img alt="">` or missing `alt`: `source/SourceSpectrum.tsx`, `stories/MaxDisagreement.tsx`, `stories/Blindspot.tsx`, `stories/StoryBackground.tsx`, `improvement/ImprovementModal.tsx`.
- ~40 hardcoded `text-right`/`pr-*`/`mr-*` instead of logical `text-start`/`pe-*`/`me-*` — harmless on a fixed-RTL app; matters if you ever render LTR content inline.
- `date-fns` + `date-fns-jalali` ship ~33 KB for ~2 utility calls — easy candidate for trimming if bundle becomes a concern.
- A handful of `"use client"` components that render purely static markup (e.g. `improvement/RaterOnboarding.tsx`) could be server components — micro-optimization.

---

## 6. UX & product

### Verified state
- Header nav is **fully commented out** at `Header.tsx:91-134` — users land on the homepage with no visible path to `/stories`, `/sources`, `/blindspots`, `/lab`. The comment says "re-enable by uncommenting", which reads like TODO-work left over.
- `/improve` page redirects to `/rate` via client-side `router.replace()` in `useEffect` — the "stuck on a blank page" agent claim was overstated: the text *does* say "در حال انتقال به صفحه بازخورد…". Minor flash, not broken.
- `WordsOfWeek` accepts `prefetchedData` prop from the home server component — if the upstream fetch fails, component renders nothing silently (agent claim is correct but low-severity).

### Blockers
| # | Finding | Action |
|---|---|---|
| U1 | No site navigation reachable from the home page (Header nav commented) | Decide whether this is intentional. If not: uncomment `Header.tsx:91-134` + mobile toggle block at 110-117 and 121-134 |

### Risks
| # | Finding | Location | Action |
|---|---|---|---|
| U2 | No loading skeletons — `TelegramDiscussions`, `StoryAnalysisPanel`, `WordsOfWeek` render nothing while upstream fetch is pending | Components listed above | Render a 1-line skeleton / low-opacity placeholder so empty slots aren't mistaken for "nothing here" |
| U3 | Analyst / methodology opacity — no public-facing page explaining who the raters are or how bias scoring works, despite it being the product's core differentiator | no page exists | Create `/about` or `/methodology` in each locale; link from story-detail sidebar |
| U4 | Dark mode = `media` query, no user toggle | `tailwind.config.ts:4` | Switch to `darkMode: "class"` and add a sun/moon button to Header (once nav is uncommented) |
| U5 | Feedback UX has 3 overlapping entry points (`بازخورد`, `ارزیابی`, improvement buttons) with unclear division | story detail page | Hide rating UI from non-invited users; unify remaining two into a single "Report an issue" modal |

### Nice-to-have
- Persian numerals inconsistent — `toFa()` utility exists but not applied uniformly (some stats in fa, some in en).
- `/stories-beta` carousel is built and working but unlinked from anywhere — users can't discover it.
- No `error.tsx` boundary on the story route — bad story IDs show a generic "خطا در بارگذاری" rather than a real 404.
- Video autoplay in the stories carousel has no `preload="none"` / click-to-play guard.

## 7. Data quality & coverage

### Risks
| # | Finding | Location | Action |
|---|---|---|---|
| D1 | Blindspot logic (`state_pct < 10 ∨ diaspora_pct < 10`) is binary and breaks on small clusters — a 3-article story with 1 diaspora voice reads as "not a blindspot" at 33% | `backend/app/services/clustering.py` | Swap to absolute-count rule for small stories: `minority_count == 1 ∧ article_count < 6 → blindspot` |
| D2 | ~~`editorial_context_fa` is written by the Niloofar persona but not exposed~~ | — | **Verified false positive**: Niloofar runs as a maintenance pipeline step (`auto_maintenance.py:2982`), writes `{context, generated_at, model}` to the column, the API schema includes it (`schemas/story.py:47`), and the frontend already renders it as a "زمینه خبر" callout (`stories/[id]/page.tsx:149-154`). Wired end-to-end. |
| D3 | Trending score = `article_count * recency_factor` with no diversity weighting. Coordinated state-media echo chamber (Tasnim+Fars+Mehr same press release) climbs above diverse coverage | `backend/app/services/clustering.py` | Weight by source diversity (distinct alignment axes) and optionally by average emotional-language score |
| D4 | Title normalization is LLM-only with no review gate. `is_edited` locks the title but there's no "low-confidence → flag for review" path | `services/story_analysis.py`, Story model | Log LLM confidence per title; route low-confidence to a review queue on the dashboard |
| D5 | Translation (fa↔en) ships without review loop. Named entities and Persian idioms often garbled for English users | `services/translation.py` | Log translations + flag mismatched named-entity counts between source and translation |
| D6 | Source seed lacks **Kurdish, ethnic-minority, and reformist-religious** outlets — current axis heavily biased to state-vs-diaspora binary | `backend/app/services/seed.py` | Add 3-5 sources: Kurdish (Rudaw FA), a left/independent voice, a reformist-religious or Qom commentator channel |
| D7 | Telegram channel mix is aggregator-heavy (~4 mega-aggregators echo the same RSS you already ingest) — signal diluted | `backend/app/services/seed_telegram.py` | Down-weight aggregator posts in telegram_analysis; overweight commentary + specialty channels |

### Nice-to-have
- No `last_contributed_at` on Analyst — can't see which of the 17 are still active.
- No edit-audit trail on Story — `is_edited=True` toggles, but there's no "who changed what, when" log.
- `journalist_report.json` script is one-off; no automation.
- The `Topic` / `TopicArticle` tables power `/lab` routes but that whole surface is an experiment (see §8).

---

## 8. Tech debt & roadmap

### Blockers
| # | Finding | Location | Action |
|---|---|---|---|
| T1 | Header nav fully commented out (also UX U1) — users have no visible navigation from the homepage | `frontend/src/components/layout/Header.tsx:91-134` | Uncomment or redesign; decide explicitly |
| T2 | Orphaned social-posting infra: full `post_to_twitter/instagram/whatsapp/bluesky/linkedin` stack with ~12 env vars, **zero callers** — confuses config and surface area | `backend/app/services/social_posting.py`, `config.py` | Either delete (Telegram is the only actual channel) or move to `backend/scripts/experimental/` with a README |

### Risks
| # | Finding | Location | Action |
|---|---|---|---|
| T3 | `/stories-beta` is the mobile carousel — fully built per the CLAUDE.md spec but unlinked from anywhere. Decide: promote to `/stories`, keep as alpha, or retire | `frontend/src/app/[locale]/stories-beta/` | Product decision needed |
| T4 | `/lab` (Topics + analyst perspectives) is labeled experimental in DEVLOG but has a full CRUD + a public route. Unclear whether it's shipping or sandbox | `frontend/src/app/[locale]/lab/`, `backend/app/api/v1/lab.py` | Same: either promote with proper discovery or move under `/experiment/` |
| T5 | No backend test suite — covered by B2; referenced here as a deliberate backlog item | `backend/tests/` | Start with API smoke tests + NLP-pipeline assertions; aim for 20–30% coverage |
| T6 | Risk register has 4 open items that are each ~30 min of work: spending cap on OpenAI, UptimeRobot, automated daily backup, Cloudflare Bot Fight | `project-management/RISK_REGISTER.md` | Work the list; these are high-leverage |

### Nice-to-have
- ~5 persona-specific scripts (`dariush_*`, `kamran_*`, `mina_*`, `reza_*`, `sara_*`) generate JSON reports that no endpoint consumes. Either surface them in `/lab` or archive.
- Dead components (also F1): `HeaderAnimation.saved.tsx`, `FeedbackWrapper.tsx`, `FeedbackModeBar.tsx`.
- `ONBOARDING.md` predates the dashboards, `/lab`, and `/stories-beta` — doesn't reflect current shape.
- Migration list is clean (15 revisions, no stranded columns) — good news.
- Dependency list is clean aside from unused social-API SDKs covered by T2.

---

## Summary — what I'd do next

### Already done this session (14 fixes landed on `main`)
Security: removed hardcoded dashboard password across 5 pages, gated `POST /social/channels`, tightened `.gitignore`, deleted stray env backups, wired the token-validation flow.
Pipeline: Celery time limits, Redis single-flight lock, ingest per-source try/except, bias-scoring `temperature=0`, silent-exception logging, aged-orphan counter.
DX: `.github/workflows/ci.yml` typecheck + import smoke; Telegram analysis cache TTL + admin invalidate.

### Needs *you*, not me
- **Railway env**: remove stale Vercel URL from `CORS_ORIGINS` (S4).
- **Product decision**: header nav — uncomment, redesign, or stay hidden? (U1 / T1)
- **Product decision**: `/stories-beta` → `/stories`, or delete? (T3)
- **Product decision**: `/lab` as a shipped feature, or park under `/experiment/`? (T4)
- **Product decision**: nuke the orphaned social-posting stack, or keep for later? (T2)

### Recommended next sprint (prioritized)
1. **Blindspot logic fix** (D1) — 20 min; your core product claim depends on this being right on small stories.
2. **Editorial context exposed** (D2) — Niloofar already writes this; just plumb it to the API + UI. High-leverage.
3. **Source coverage gap** (D6) — add 3-5 Kurdish / reformist / religious sources. Broadens the axis meaningfully.
4. **Backend test suite** (B2 / T5) — start small: one test per pipeline stage with LLM mocks.
5. **RISK_REGISTER cleanup** (T6) — 4 items, each ~30 min, high impact.

### Bigger bets to consider later
- LLM-computed `dispute_score` (already tracked in memory) to replace the `|state_pct − diaspora_pct|` proxy.
- Trending diversity weight — changes what rises on the homepage.
- Analyst activity surface — `last_contributed_at` + public "about our analysts" page. Directly addresses credibility opacity.
